# ─────────────────────────────────────────────────────────────────────────────
# FitForge — ai-agent-service Service
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: ai-agent-service
  namespace: fitforge
  labels:
    app.kubernetes.io/name: ai-agent-service
    app.kubernetes.io/part-of: fitforge
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-agent-service
  ports:
    - name: http
      port: 5006
      targetPort: 5006
