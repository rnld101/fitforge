apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: fitforge
  labels:
    app.kubernetes.io/name: user-service
    app.kubernetes.io/part-of: fitforge
    app.kubernetes.io/component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: user-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: user-service
        app.kubernetes.io/part-of: fitforge
        app.kubernetes.io/component: backend
    spec:
      containers:
        - name: user-service
          image: aswin018/k8s_mern_app_user-service:v2
          ports:
            - containerPort: 5002
              name: http
          envFrom:
            - configMapRef:
                name: user-service-config
            - secretRef:
                name: fitforge-app-secrets

          startupProbe:
            httpGet:
              path: /users/health
              port: 5002
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12

          livenessProbe:
            httpGet:
              path: /users/health
              port: 5002
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /users/health
              port: 5002
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3

          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi