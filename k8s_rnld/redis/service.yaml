apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: fitforge
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/part-of: fitforge
    app.kubernetes.io/component: cache
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379